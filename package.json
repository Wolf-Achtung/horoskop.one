{
  "name": "horoskop.one-gold",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/build.mjs --watch",
    "build": "node scripts/build.mjs",
    "clean": "node -e \"(async()=>{const fs=require('fs');fs.rmSync('dist',{recursive:true,force:true});})();\""
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}