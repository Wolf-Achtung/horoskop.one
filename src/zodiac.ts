export function drawZodiacWheel(canvas){ const ctx=canvas.getContext('2d'); const W=canvas.width,H=canvas.height,cx=W/2,cy=H/2,R=Math.min(W,H)/2-14;
  const g=ctx.createRadialGradient(cx,cy,R*.1,cx,cy,R); g.addColorStop(0,'#0f1930'); g.addColorStop(1,'#0a0f1c'); ctx.fillStyle=g; ctx.fillRect(0,0,W,H);
  ctx.strokeStyle='rgba(255,255,255,.28)'; ctx.lineWidth=2; ctx.beginPath(); ctx.arc(cx,cy,R,0,Math.PI*2); ctx.stroke(); ctx.beginPath(); ctx.arc(cx,cy,R*.68,0,Math.PI*2); ctx.stroke();
  for(let i=0;i<360;i+=30){ const a=(i-90)*Math.PI/180; const x1=cx+Math.cos(a)*R, y1=cy+Math.sin(a)*R, x2=cx+Math.cos(a)*(R-16), y2=cy+Math.sin(a)*(R-16); ctx.beginPath(); ctx.moveTo(x1,y1); ctx.lineTo(x2,y2); ctx.stroke(); }
  ctx.globalAlpha=.5; for(let i=0;i<80;i++){ const x=Math.random()*W,y=Math.random()*H,r=Math.random()*1.4; ctx.fillStyle='#fff'; ctx.beginPath(); ctx.arc(x,y,r,0,Math.PI*2); ctx.fill(); } ctx.globalAlpha=1; ctx.fillStyle='#e1c07a'; ctx.beginPath(); ctx.arc(cx,cy,2.4,0,Math.PI*2); ctx.fill(); }